import { Sequelize } from 'sequelize-typescript'
//import PersonRepository from '../repositories/personRepository'

export default (sequelize: Sequelize) => ({
  //person: new PersonRepository(sequelize),
})